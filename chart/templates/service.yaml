apiVersion: v1
kind: Service
metadata:
  name: {{ include "space-cadet.fullname" . }}
  labels:
    {{- include "space-cadet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "space-cadet.selectorLabels" . | nindent 4 }}
